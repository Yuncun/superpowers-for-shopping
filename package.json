{
  "name": "superpowers-for-shopping",
  "version": "1.0.1",
  "description": "Low-friction shopping for Claude Code: query → populated carts → one Review button.",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "bin": {
    "cart-flow": "./bin/cart-flow.js",
    "cart-profile-flow": "./bin/cart-profile-flow.js"
  },
  "scripts": {
    "test": "node --test 'test/**/*.test.js'"
  },
  "dependencies": {
    "js-yaml": "^4.1.0"
  },
  "devDependencies": {
    "playwright": "^1.60.0"
  }
}
